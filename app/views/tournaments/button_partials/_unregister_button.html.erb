<%= button_to 'Unregister in tournament',
              team_tournament_path(@current_user.team.find_team_tournament(tournament.id)),
              method: :delete,
              remote: true,
              class: 'unregister_button btn btn-danger' %>